sonar.projectKey=insightify-app
sonar.projectName=Insightify App

# Analyze our own source only — never node_modules, build output, or migrations.
sonar.sources=backend/src,frontend/app,frontend/components,frontend/lib
sonar.exclusions=**/node_modules/**,**/.next/**,**/dist/**,**/build/**

sonar.sourceEncoding=UTF-8
